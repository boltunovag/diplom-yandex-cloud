---
- name: Check Zabbix and provide info
  hosts: zabbix.ru-central1.internal
  become: yes
  
  tasks:
    - name: Wait for Zabbix web
      wait_for:
        port: 80
        timeout: 300
        
    - name: Display access info
      debug:
        msg: |
          ✅ Zabbix is running!
          🌐 URL: http://{{ ansible_host }}/
          👤 Login: Admin
          🔑 Password: zabbix
          
          🔧 Manual configuration required:
          1. Log in to Zabbix
          2. Go to Configuration → Actions
          3. Set event source to "Auto registration"
          4. Create action "Auto register Linux servers"
          5. Condition: Host metadata contains "Linux"
          6. Operations: Add host, Add to group, Link to template
          
          ⏰ Wait 2-3 minutes for hosts to appear