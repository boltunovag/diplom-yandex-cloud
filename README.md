# Дипломный проект: Отказоустойчивая инфраструктура в Yandex Cloud

**Статус проекта:** <img src="https://img.shields.io/badge/Status-Завершен-success?style=flat-square" alt="Статус"> <img src="https://img.shields.io/badge/Terraform-Used-blue?style=flat-square&logo=terraform" alt="Terraform"> <img src="https://img.shields.io/badge/Ansible-Used-red?style=flat-square&logo=ansible" alt="Ansible">

## 📋 Оглавление

- [Описание проекта](#-описание-проекта)
- [Цели и задачи](#-цели-и-задачи)
- [Архитектура](#-архитектура)
- [Стек технологий](#-стек-технологий)
- [Быстрый старт](#-быстрый-старт)
- [Развертывание инфраструктуры](#-развертывание-инфраструктуры)
- [Настройка с помощью Ansible](#-настройка-с-помощью-ansible)
- [Доступ к сервисам](#-доступ-к-сервисам)
- [Резервное копирование](#-резервное-копирование)

## 🎯 Описание проекта

Дипломный проект представляет собой автоматизированное развертывание отказоустойчивой инфраструктуры для веб-сайта в Yandex Cloud. Инфраструктура включает в себя балансировщик нагрузки, веб-серверы, систему мониторинга на основе Zabbix, а также стек для сбора и анализа логов (Elasticsearch, Kibana).

## 🎯 Цели и задачи

**Ключевая задача** — разработать отказоустойчивую инфраструктуру для сайта, включающую мониторинг, сбор логов и резервное копирование основных данных[citation:2].

**Реализованные требования:**
- [x] **Сайт:** Два веб-сервера с nginx во внутренней сети, доступ через Application Load Balancer.
- [x] **Мониторинг:** ВМ с Zabbix, агенты на всех серверах, настроенные дашборды.
- [x] **Логи:** ВМ с Elasticsearch и Kibana, сбор логов с веб-серверов через Filebeat.
- [x] **Сеть:** Один VPC, разделение на публичные и приватные подсети, Bastion host для доступа.
- [x] **Безопасность:** Настроены Security Groups, запрет на публикацию токенов.
- [x] **Резервное копирование:** Настроены снапшоты дисков ВМ с ограничением времени жизни.

## 🏗 Архитектура

<!-- Опишите свою инфраструктуру. Вот пример, который нужно заменить на свой: -->

Проект развертывается в Yandex Cloud и имеет следующую архитектуру:

- **Yandex Application Load Balancer** — принимает внешний HTTP-трафик и распределяет его между веб-серверами.
- **Веб-серверы (web-1, web-2)** — виртуальные машины в *приватной подсети*, на которых развернуты nginx и Zabbix Agent.
- **Bastion host** — виртуальная машина в *публичной подсети* с белым IP, предназначенная для безопасного SSH-доступа к внутренним ресурсам.
- **Zabbix** — виртуальная машина в *публичной подсети* для мониторинга инфраструктуры.
- **Elasticsearch & Kibana** — виртуальные машины для приема, хранения и визуализации логов.

Для обеспечения связи между компонентами и выхода в интернет из приватных сетей настроены:
- NAT Gateway
- Таблица маршрутизации

## ⚙️ Стек технологий

| Компонент | Технология | Комментарий |
|-----------|------------|-------------|
| **Инфраструктура как код (IaC)** | Terraform | Версия 1.5+ |
| **Конфигурационный менеджмент** | Ansible | Версия 2.14+ |
| **Облачный провайдер** | Yandex Cloud | Compute Cloud, VPC, ALB |
| **Контейнеризация** | Docker | Для Elasticsearch, Kibana, Zabbix и Filebeat |
| **Веб-сервер** | Nginx | На веб-серверах |
| **Мониторинг** | Zabbix | В Docker-контейнерах |
| **Сбор и анализ логов** | ELK Stack (Elasticsearch, Kibana, Filebeat) | В Docker-контейнерах |
| **Виртуальные машины** | Yandex Compute Cloud | Минимальные конфигурации, прерываемые |



---

## ⚠️ Известные проблемы и их решения

### Проблема с доступом к репозиториям Elastic
**Симптом:**  
403 Forbidden при попытке скачать пакеты из https://artifacts.elastic.co

**Причина:**  
Elastic изменил политику доступа к своим репозиториям. Начиная с 2022 года, требуется аутентификация даже для бесплатных версий.

**Решение:**  
Все компоненты ELK-стека (Elasticsearch, Kibana, Filebeat) развернуты с использованием Docker-контейнеров:
- Elasticsearch и Kibana используют официальные образы `docker.elastic.co`
- Filebeat использует образ `docker.io/elastic/filebeat`
- Для обхода проблем с доступом применены настройки `container_default_behavior: compatibility`

### Проблема с vm.max_map_count для Elasticsearch
**Симптом:**  
sysctl "vm.max_map_count" is not in a separate kernel namespace

**Решение:**  
Добавлена задача установки параметра ядра до запуска контейнера:
```yaml
- name: Set vm.max_map_count for Elasticsearch
  sysctl:
    name: vm.max_map_count
    value: "262144"
    sysctl_set: yes
    state: present
    reload: yes

**Симптом:**
Host key verification failed при попытке подключиться к web-1.ru-central1.internal

**Решение:**
Все Ansible-плейбуки запускаются с bastion-хоста, что позволяет использовать FQDN-имена без ProxyCommand. Для локального доступа используйте:
  ```bash
     ssh -i ~/.ssh/yc-ed25519 ubuntu@$(terraform output -raw bastion_external_ip)
     ssh ubuntu@web-1.ru-central1.internal


## 🚀 Быстрый старт

### Предварительные требования

1. **Аккаунт в Yandex Cloud** с активным биллингом.
2. **Сервисный аккаунт** с правами: `editor`, `vpc.user`, `compute.images.user`.
3. **SSH-ключи** для доступа к виртуальным машинам.

### Подготовка к развертыванию

1. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/boltunovag/diplom-yandex-cloud 
   cd diplom-yandex-cloud```


2. Подготовьте файлы с ключами:
- Поместите файл ключа сервисного аккаунта (key.json) в корень проекта.
- Убедитесь, что SSH-ключ yc-ed25519 находится в ~/.ssh/.

### 🛠 Развертывание инфраструктуры

Развертывание инфраструктуры осуществляется с помощью Terraform.

1. Инициализация Terraform:
   ```bash
    terraform init

2. Просмотр плана развертывания:
   ```bash
    terraform plan

3. Применение конфигурации:
   ```bash
    terraform apply

После успешного выполнения команды terraform apply в выводе появятся внешние IP-адреса Bastion и Zabbix.

### ⚙️ Настройка с помощью Ansible

Для итоговой конфигурации серверов используется Ansible. Запускайте плейбуки с Bastion-хоста, так как в файле inventory.yml используются FQDN имена вида *.ru-central1.internal, которые резолвятся только внутри облачной сети.

1. Скопируйте файлы проекта на Bastion:

   ```bash
    scp -i ~/.ssh/yc-ed25519 -r ./ansible/ ubuntu@<BASTION_IP>:~/
    scp -i ~/.ssh/yc-ed25519 key.json ubuntu@<BASTION_IP>:~/ansible/

2. Подключитесь к Bastion и запустите плейбуки:

   ```bash
    ssh -i ~/.ssh/yc-ed25519 ubuntu@<BASTION_IP>
    cd ansible
    ansible-playbook setup-webservers.yml
    ansible-playbook zabbix-setup.yml
    ansible-playbook zabbix-agents.yml

### 🌐 Доступ к сервисам

После успешного развертывания будут доступны следующие сервисы:

| Сервис    | URL                      | Описание                                       |
|:----------|:-------------------------|:-----------------------------------------------|
| Веб-сайт  | `http://<ALB_IP>`        | Основной сайт, развернутый за балансировщиком  |
| Zabbix    | `http://<ZABBIX_IP>`     | Веб-интерфейс системы мониторинга<br>**Логин:** `Admin`<br>**Пароль:** `zabbix` |

| Kibana    | `http://<KIBANA_IP>:5601`| Веб-интерфейс для анализа логов                |

### Важно: Все веб-серверы и Elasticsearch находятся в приватных подсетях и недоступны напрямую из интернета. Доступ к ним осуществляется только через Bastion host.

💾 Резервное копирование
Для основных дисков всех виртуальных машин настроено автоматическое ежедневное резервное копирование с помощью снапшотов. Время жизни снапшотов ограничено одной неделей.


### <i>i!</i> Примечание

В ходе развёртки возникла ошибка доступа к официальным репозиториям Elastic (403 Forbidden). Для решения проблемы были использованы публичные образы Bitnami для Elasticsearch и Kibana, а для Filebeat применён флаг [trusted=yes] при добавлении APT-репозитория, что позволило обойти проблему с отсутствующей подписью. Это приемлемо в учебной среде, но требует дополнительных мер безопасности в production. 



---

## ✅ Безопасность

```markdown
## 🔒 Безопасность

### Принципы безопасности, реализованные в проекте:

1. **Разделение сетей**:
   - Публичная подсеть: только bastion, zabbix, kibana
   - Приватная подсеть: веб-серверы, elasticsearch
   - Все внутренние серверы недоступны напрямую из интернета

2. **Минимизация прав доступа**:
   - Security Groups настроены с принципом "минимальных привилегий"
   - Доступ к приватным серверам возможен только через bastion
   - Порты открыты только для необходимых сервисов

3. **Защита конфиденциальных данных**:
   - SSH-ключи хранятся локально, не коммитятся в репозиторий
   - Ключи сервисного аккаунта исключены из git
   - Все пароли используют значения по умолчанию только для демонстрации

4. **Обновление системы**:
   - Все ВМ получают последние обновления безопасности
   - Docker-контейнеры используют последние версии образов

> **Важно**: Для production-среды необходимо заменить все пароли по умолчанию, настроить HTTPS и использовать более строгие политики безопасности.


---

## 🎥 Демонстрация работы


1. **Балансировка нагрузки**:
   ```bash
   for i in {1..10}; do curl http://$(terraform output -raw load_balancer_ip); done


## 🚀 Дальнейшее развитие проекта

Предложения по улучшению инфраструктуры:

1. **Масштабирование**:
   - Добавление автоматического масштабирования групп веб-серверов
   - Настройка multi-AZ развертывания для критических сервисов

2. **Безопасность**:
   - Настройка HTTPS с автоматическим обновлением сертификатов
   - Реализация WAF (Web Application Firewall) для защиты веб-серверов
   - Настройка SSO (Single Sign-On) для доступа к админ-панелям

3. **Надежность**:
   - Реализация multi-region архитектуры
   - Настройка репликации данных Elasticsearch между регионами
   - Добавление системы аварийного переключения (failover)

4. **Автоматизация**:
   - Реализация CI/CD пайплайна для автоматического обновления инфраструктуры
   - Добавление интеграционных тестов для проверки работоспособности сервисов
   - Внедрение системы алертинга при проблемах с инфраструктурой


### 👨‍💻 Команда проекта

Болтунов Алексей — системный администратор, автор проекта.<br>
Булат Замилов - дипломный руководитель, проверяющий эксперт

⚠️ Внимание: Данный репозиторий является учебным проектом. Ключи доступа и чувствительные данные из него исключены.
